# Portable Reflect Prompt

Run the reflect workflow for this workspace.

1. Read `<workspace-root>/.agents/workflows/cortex/reflect.md`.
2. Read the references in `<workspace-root>/.agents/workflows/cortex/references/`.
3. Use saved files and graph reads when available.
4. Append one reflect log entry.
5. Report the top 3 insights, file updated, recommended next actions, and what was checked.
